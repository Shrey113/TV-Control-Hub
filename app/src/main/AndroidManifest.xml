<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ====== NETWORK PERMISSIONS FOR TV DISCOVERY ====== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    
    <!-- Required for mDNS TV discovery on Android 13+ -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVControlHub"
        android:usesCleartextTraffic="false"
        android:hardwareAccelerated="true">
        
        <!-- ====== MAIN ACTIVITY - TV List ====== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TVControlHub"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ====== SHORTCUT INIT ACTIVITY (Invisible) ====== -->
        <activity
            android:name=".ShortcutInitActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:taskAffinity=""
            android:launchMode="singleInstance" />
            
        <!-- ====== TV REMOTE ACTIVITY (Standalone) ====== -->
        <activity
            android:name=".TvRemoteActivity"
            android:exported="false"
            android:theme="@style/Theme.TVControlHub"
            android:taskAffinity=""
            android:launchMode="singleTask" />

    </application>

</manifest>